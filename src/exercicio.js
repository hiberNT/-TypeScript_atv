"use strict";
const numeros = (num, num2) => num * num2;
const resultado = numeros(38, 42);
console.log(resultado);
const nome = (saudacao, nome) => saudacao + ' ' + nome;
const chamar = nome('ola', 'João');
console.log(chamar);
